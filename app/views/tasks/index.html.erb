<h1>やることリスト</h1>

<ol>
  <% @tasks.each do |task| %>
  <li>
      <%= link_to task.content, task_path(task) %> &gt; <%= task.status %>
  </li>
  <% end %>
</ol>

<%= link_to "タスクを追加する", new_task_path %>